# Earnings ideas ledger — every idea ever proposed, one line each

Status vocabulary: `proposed` (desk filed it, CoS has not ruled) · `adopted` (in `pipeline.json` with an owner) · `parked` (with its revive trigger) · `declined` (with the reason) · `live` (shipped) · `retired` (was live, stopped) · `never` (a standing rule forbids it — listed so it is not re-proposed).

**The desk reads this whole file before proposing.** Re-proposing a line here without a materially new mechanism is a defect. Seeded 2026-09-18 by the CoS from everything on the record.

| # | Date | Idea | Lever | Rung | Status | Note |
|---|---|---|---|---|---|---|
| 1 | 2026-09-18 | **Faceless YouTube channel** — Mo recalls this from an earlier session; the details are not on file. Sketch: the nightly tape and Tuesday board already render as PNGs (`tools/x-images/make.py`, `og/x/board-latest.png`); a 45–60 s vertical "tonight's tape" with synthetic voice, one per night, links to the board and the index pages. | clicks | M1 | **proposed** | Desk: re-derive as a full nine-line idea on its first run; the open questions are channel economics (YouTube Shorts pay ~nothing; the value is the link and the search presence), production cost per clip, and whether the board's copy rules (R9, R10 never-post list) can be enforced on a script. Mo to add anything he remembers. |
| 2 | 2026-09-12 | Paid contributors (Seeking-Alpha style articles/advice) | clicks | M2 | **parked** | Mo: "not sure we are ready… maybe when we get some more ROI." Revive trigger: M0 held two consecutive months AND returning ≥ 20% for 4 weeks (`claude/cos/ROADMAP.md`). |
| 3 | 2026-09-17 | Sealed-product buy strip above the fold on every commercial page (R11) | $/click | M0 | **live** | Shipped Sep 17 on 58 pages + the 30th module; Gengar Monday beat grades it; per-product EPN custom IDs. First clean read Sep 18+. |
| 4 | 2026-09-18 | Deep-link buy strips and engine blocks to the **cheapest clean live listing** instead of a search URL | $/click | M0 | **adopted** | From the gap analysis G3 (listing-level links converted 39% on the Sep 4 read). Wed build session. |
| 5 | 2026-09-18 | Dashboard (`/`) eBay links + `click` telemetry + distinct `customid=home-*` | $/click | M0 | **adopted** | Gap analysis G1. Wed build session. |
| 6 | 2026-09-15 | Triggered "Signal Alert" email (one send/day max, real crossings only) | clicks | M0 | **parked** | Approved by Mo; gated on 40 confirmed subscribers (email program). |
| 7 | 2026-09-17 | Weekly Tape Recap email to the list | clicks | M0 | **live** | 4 active subscribers; "send it every week or cancel it" (email program). |
| 8 | 2026-09-18 | RSS/JSON feed of the nightly marks + Tuesday Tape (`/feed.xml`) | clicks | M0 | **proposed** | Repo scan §3.9. Cheap; word-of-mouth surface for investors who won't give an email. |
| 9 | 2026-09-08 | Auction Desk (`/auctions`, `customid=auctions`) as the intent lever | $/click | M0 | **live** | Bid watch only until close-time capture exists; no ask-vs-sold gap figure may be published. |
| 10 | 2026-08 | Amazon Associates on `/best-card-supplies` | $/click | M0 | **live, at risk** | Needs 3 qualifying sales by ~late Dec 2026 or the account closes. Decision at the Oct 1 roster review: earn them or convert the page to eBay deliberately. |
| 11 | 2026-09-04 | Bowman set indices per set (BOW26, BCB26) as the high-end-chaser destination | $/click | M1 | **live** | Mo's thesis: high-end buyers want the indices; per-set, fixed universe. |
| 12 | 2026-09-15 | Sector-model Pokémon set indices (SV151 first) → each index page sells the set's sealed product | $/click | M1 | **adopted** | SV151 rebuild in progress (screen run 207/207 Sep 17). Sealed rows live on the 5 Pokémon indices since Sep 13. |
| 13 | 2026-09-15 | Zero-knowledge Vault sync (cross-device, no accounts) | clicks | M1 | **adopted** | Retention; W4 Oct 6–12. Cashes out only via return visits → clicks; the desk should not re-propose accounts/PII variants (ruled out). |
| 14 | 2026-09-16 | One real off-site link **submission** (not a comment) — board ranking-rule change or the 151 index as the material — measured on GA4 referral sessions | clicks | M1 | **adopted** | Needs Mo's human account. The r/baseballcards and 151 drafts are held (numbers retracted / page not live). |
| 15 | 2026-09-17 | X account run by Grok Bot (vendor) with CoS direction; link in the main post | clicks | M1 | **live** | R10: nothing on this project posts. Ideas may brief the vendor via the CoS only. |
| 16 | 2026-07 | Card Dungeon "YouTube Break Monitor" (detect break videos before price spikes) | clicks | M1 | **parked** | Dungeon-era spec (Mo's persistent layer, hands-off). Revive only as a desk idea with a concrete mechanism and a source of break-video data. |
| 17 | 2026-07 | Card Dungeon "Discord Hype Aggregator" | clicks | M1 | **declined** | No data source we can read without scraping private servers. |
| 18 | 2026-07 | Card Dungeon "Wax Variant Tracker" (hobby vs retail vs blaster spread) | $/click | M1 | **parked** | Overlaps the sealed rows + buy strips; revive if a per-variant sealed row proves out on one Pokémon index. |
| 19 | 2026-07 | PSA grading ROI calculator / hobby-box ROI calculator | $/click | M0 | **live** | `/hobby-box-roi-calculator`, `/psa-grading-guide` exist; both are R11-exempt utility pages. |
| 20 | 2026-09-08 | "My AI bids on cards" — SportsCardRadio's posts, flagged by Mo for review | — | — | **proposed** | Mo asked Claude to watch @SportsCardRadio and @WatchTheBreaks for AI-in-cards ideas that fit. Desk: read what they actually do (public posts only) and file the one mechanism that fits our engine, or decline. |
| 21 | — | Topps direct affiliate | — | — | **retired** (Aug 23) | Product sells out in minutes; clicks can't convert. Do not retry. |
| 22 | — | Fanatics Collect affiliate | — | — | **retired** (Aug 23) | Same sold-out-inventory logic. Do not retry. |
| 23 | — | Google Ads / paid acquisition | — | — | **never** | Ruled out Jul 28; $0.28/click cannot clear any CPC. |
| 24 | — | AdSense / display ads | — | — | **never** | Competes with affiliate clicks, junks UX (Jul 28). |
| 25 | — | Paywall / subscription / charging readers | — | — | **never** | Mo's value: free advice, compensated infrastructure (Aug 19). |
| 26 | — | Kit (ConvertKit) as a second email list | — | — | **retired** (Sep 3) | MailerLite is the one list; never propose a second. |
| 27 | — | Publishing Mo's COMC positions / inventory on the site | — | — | **never** | Sep 16 ruling; "small potatoes, loses site trust." |
| 28 | — | Hammer/auction-close prices as a tape on Home | $/click | M0 | **parked** | Ruled no chart; two-number stat tile only, and nothing until close-time capture is fixed (Sep 16 retraction). |
| 29 | 2026-09-18 | GA4 read-only service account so cloud runs read analytics daily | — | M0 | **retired** (superseded 2026-09-19) | Superseded by the keyless build: GitHub Actions → OIDC → `ga4-reader` shipped 2026-09-18 and every cloud lane now reads `ga4-latest.json` nightly. No service-account key exists or is needed; do not re-propose. |
| 30 | 2026-09-19 | **Engine-block the star-player pages** — add each page's "Top card" (Messi Megacracks #71 first; Ronaldo, Ohtani, Wembanyama, Flagg, Haaland, Yamal, Misiorowski, PCA) to `data/watchlist.json` so the pages that actually pay get the live verified-listing + live-auction rail with per-listing deep links; tag body links per card | $/click | M0 | **adopted** (CoS 2026-09-19) | `claude/ideas/2026-09-19.md`. Evidence: EPN Sep 4–17 `lionel-messi-cards` 4 clicks → $860 → $25.82 while every engine-hosted ID earned $0. ESTIMATE +$15–$55/mo at today's traffic. **Owner: Wednesday build session (week of Sep 21); pipeline proposal at the Sep 23 weekly. The 8 watchlist adds are tracked-card additions = Needs-Mo (Sep 3 rule) — queued in NEEDS-MO as one yes; the per-card body-link customids are auto-approved HTML and proceed regardless. — Mo APPROVED in chat 2026-09-19, no vetoes; Needs-Mo cleared, Wednesday build proceeds.** Slug only after a verified mark (30th rule). Kill: Oct 21 read, no action on ≥30 clicks or <4/8 cards clear the floor. **STARTED 2026-09-21 (`b84272e`): 3 of 8 in the nightly engine tonight — Ohtani 2018 Update #US285 (35 verified asks), Wembanyama 2023-24 Prizm #136 base (31), Yamal 2023-24 Topps Chrome UCC #64 (10) — no slug until the block generators can name a non-Bowman set (they refuse cardType `single` until then). NOT engine cards, measured: Messi Megacracks #71 (1 raw ask, the market is graded — the $860 sale could never have been an engine card) and Ronaldo Mega Craques #137 (0); PCA and Misiorowski 1st autos are auction-only (0 verified); Haaland UCL #74 has 1 ask; Flagg #251 is an R8 question (titles split "Topps Chrome" / "Topps Chrome Update"). Wednesday build: teach `facts()` the set, add slugs after marks, and treat Messi/Ronaldo/Misiorowski as GRADED-link pages (#31/#33), not engine pages. The 4-of-8 floor in the kill criterion is already in doubt — say so at the Oct 21 read rather than lowering it.** |
| 31 | 2026-09-19 | **Raw · PSA 9 · PSA 10 text line under the buy strip on single-card pages**, one custom ID per grade, no figures shown | $/click | M0→M1 | **adopted** (CoS 2026-09-19) | `claude/ideas/2026-09-19.md`. Evidence: Holliday #CPA-EH raw $104 vs one dated PSA 10 sale $720 (~7×, n=1); the Holliday strip already targets PSA 10. Text line, not tiles (Mo's Sep 18 "too busy"). **Owner: Wednesday build (schema + renderer); pilot on `ethan-holliday-rookie-cards` ONLY; Gengar's Monday read grades it; roll wider only after the pilot renders clean on a phone. Mo endorsed the desk report in chat 2026-09-19.** ESTIMATE +$5–$20/mo; the first read on grade appetite. Kill: Oct 21, graded IDs <10% of page clicks. **Note (2026-09-20): #33 shipped the AG variant of this link on four OTHER pages and found the Holliday PSA 10 query returns 0 results under an AG filter — a thin book. Worth knowing before the graded line pilots there.** |
| 32 | 2026-09-20 | **"Sealed cases →" link beside every box/ETB/hobby-box link** — a `case` object in `data/buy-strip.json`, case-shape query, `customid=<page>-case`, **no figure** (a case is never a mark — `TITLE_BAD_SEALED` stands) | $/click | M0 | **live** (shipped 2026-09-20, `7d0f85f`) | `claude/ideas/2026-09-20.md`. Every sealed query on the site excludes `-case -lot`, so the biggest sealed basket had never had a link. **Shipped the same day Mo approved it, not at the Sep 28 build — it is config plus one renderer branch, and the supply check had to be live anyway.** 26 sealed pages measured on `/api/comps`; **13 shipped, 13 dropped for <3 clean case listings** — including **all six Pokémon sealed rows (0–2 listings) and all five Pokémon guide strips**, so no Pokémon page carries one and `build-sealed-rows.mjs` was left untouched rather than shipping dead code. Two traps now held in `tools/case-shelf.mjs`: all 30 raw hits for Topps Definitive were **$20–$40 "CASE BREAK" slots**, and a loose word match let a "bowman chrome" link land on a plain Bowman case (R8) until `must` became a contiguous phrase. Gengar's Monday beat reports THIN/DEAD under 3. Kill: Oct 21 read, `*-case` IDs 0 actions on ≥20 clicks or <5% of sealed clicks. |
| 33 | 2026-09-20 | **Authenticity-Guarantee-filtered links for $200+ singles** — eBay dropped the trading-card AG threshold $250 → $200 in Aug 2026 (VAR, Aug 1/11); `secondary.ag` adds eBay's "Show only" filter, relabels the link "Authenticated on eBay →", routes it to `customid=…-ag`, and adds one dated line of fine print naming the program as eBay's | $/click | M0 | **live** (shipped 2026-09-20, `7d0f85f`) | `claude/ideas/2026-09-20.md`. **Ship-gate passed: the parameter is `LH_AV=1`**, read off eBay's own search sidebar in a browser (the desk declared it could not verify it — cloud fetch of eBay search is 403 — and that declaration is why it was checked before shipping). **Live on 4 pages, none of them the desk's own pilot:** lebron-james-cards ($9,500 median / 19 AG), lionel-messi-cards ($4,875 / 17), cameron-boozer-rookie-cards ($2,322 / 15), victor-wembanyama-rookie-cards ($1,873 / 491). **`ethan-holliday-rookie-cards` did NOT ship** — its PSA 10 query goes **1 result → 0** under the filter and its broad link is a sub-$200 basket; the over-filtering kill criterion fired before launch. Gengar's Monday beat reports off-threshold if a median falls under $200. Kill: Oct 21, `-ag` IDs convert no better than plain on ≥30 clicks each, or an AG search returns 0 listings twice. |
| 34 | 2026-09-21 | **"−12% vs Sep 20 mark" chip on every verified fixed-price listing in the engine block's rail** — `js/engine-block.js` step 4 computes (price + shipping − mark) / mark per row from two figures already on the block, neutral colour, no adjective; **auction rows get nothing** (a bid is a 12-hour-lagged floor); under-mark rows route to `customid=card-<id>-bm` so the split is readable against plain `card-<id>` | $/click | M0 (M1 with #30) | **adopted** (CoS 2026-09-21) | `claude/ideas/2026-09-21.md`. Evidence: listing-level links converted 39% on the Sep 4 read while search links sold sub-$3; the Sep 4–17 Custom-ID read had **every engine-hosted ID at $0** on ~10 clicks — the rail gets clicks and converts none. eBay's own "Great Price" badge is the precedent (no effect size published). ESTIMATE +$0–$10/mo today, +$10–$40/mo once #30 puts $400–$900 cards in the rail. **Owner: Wednesday build session (week of Sep 21) — pairs with #30 on the same rail; pipeline proposal at the Sep 23 weekly. Build answers step (a) first: if the Browse API's `itemWebUrl` does not expose `customid` as a rewritable param, ship the chip WITHOUT the `-bm` split and read via GA4 `click` only, per the idea's own fallback — the chip's honesty rules (fixed-price rows only, never a gated mark, mark date on its face) are ship conditions.** CoS 2026-09-21 afternoon, on Mo's "anything worth implementing now": no — worth ~$0 until #30's cards are in the rail; Wednesday stands. Kill: Oct 21, `-bm` IDs no better than plain on ≥30 clicks each → drop the routing; a chip on an auction row or a gated mark is a same-day defect. |
| 35 | 2026-09-21 | **"Complete set →" link on the index pages** (`customid=<ticker>-set`, link only, no figure) — #32's shape on the singles basket: `set` object in the strip/sealed-row config, `SET_OK`/`SET_BAD` guard in `tools/case-shelf.mjs` (contiguous "complete set" / "master set"; throws out almost/partial/%/pick/complete-your/energy/promo-only/JP-CN/graded), contiguous-phrase `must` for the R8 Bowman pair, ships only at ≥3 clean listings, THIN/DEAD on Gengar's Monday beat | $/click | M0 | **parked** (CoS 2026-09-21 afternoon — was adopted the same morning; re-measured with a guard and it fails its own screen) | `claude/ideas/2026-09-21.md`. **The desk's supply screen did not survive a guard.** Re-run 2026-09-21 ~14:45 PT on production `/api/comps` with a draft `SET_OK`/`SET_BAD`: **BCB26 0 clean** (every hit is a "complete your set" single storefront, $0.99–$1.49 — the desk's "≥8 Chrome sets $150–$550" were those); **BOW26 real, but $7–$22 base sets** (100-card vet/rookie sets, Murakami RC) — about $0.45 of commission a sale, not the desk's $240–$500; **AH26 three real sets, $36 / $190 / $280** — and two $1 "master set" singles the guard needs a price floor to exclude; the $6,400 master-with-promos never appeared. Pokémon `/api/comps` must be called with `category_ids=183454` (the default category returns 0 rows for TCG sets — the desk's AH26 numbers came from somewhere else). **Revive trigger: SV151 live** — the one page where the set is the index and complete/master sets clear $2,000 (4 measured Sep 21 morning), i.e. $45–$75 a sale; ship there first with the guard + a per-set price floor, and only then re-screen AH26. Bowman set links are not worth a pixel above the fold at $0.45 a sale. |
| 36 | 2026-09-22 | **Per-card "Listings →" column on the six Pokémon index tables** (`customid=<ticker>-<num>`) — the column `bowman-chrome-2026-index.html` already renders on 204 rows, ported to PB26/CR26/AH26/PRIS25/DR25/PF25 (289 priced rows, $13.7k basket, **0 in-table eBay links today**; the only per-card link is two taps deep in a popup and every card on an index shares one custom ID); `card-img.js` upgrades the row link to the cheapest verified listing it already fetched for the thumbnail (no new API call); rows ≥ $200 (17 today) get #33's `LH_AV=1` variant; SV151 inherits at rebuild | $/click | M0 (M1 with SV151 + #5) | **live** (shipped 2026-09-22, `0a25121`) | `claude/ideas/2026-09-22.md`. Evidence: the in-table inventory (BOW26 204 vs Pokémon 0); `/destined-rivals-index` is the highest-KE-rate lander with ≥5 sessions (9 / 6 / 44%, GA4 day Sep 20). ESTIMATE +$3–$25/mo at today's index traffic. Gate risk named: these six pages are `remark-indices.mjs`'s regex-surgery pages — dry-run the re-mark on all six before pushing. **Its same-day FLAG is CLOSED: the 254 unencoded `#` links are fixed, pushed (`e15c2e8`) and live-verified on all six pages, 2026-09-22 ~09:20 PT — `#` → `%23` inside `_nkw` only, nothing else touched, `remark-indices --dry` clean on all six. A new auditor check (`epn-fragment`, 1b) reads the RAW file and FAILs on any raw `#` in an eBay URL; the desk's diagnosis was one step off — check 1 strips `<script>` blocks, so it never saw these links at all, which is a blind spot shared by every JS-built link. See `claude/cos/epn-fragment-fix-2026-09-22.md`.** **SHIPPED 2026-09-22 ~10:0x PT on Mo's go-ahead, not at the Wednesday build** (`0a25121`, live-verified: 289 cells, 289 per-card IDs, 17 AG, 0 raw `#`). Built by `tools/build-index-listings-col.mjs` (idempotent) on the new `tools/lib/epn.mjs`, now the one place an eBay affiliate URL is assembled. **Two things the idea had wrong, both caught before they shipped:** (1) the row's `data-card-img` is `ebay:<slug>`/`ph:<n>`, not a query string — the query is built from the basket name + the number **exactly as the page prints it**, padding kept: verified on eBay, `125/094` returns 753 results and `125/94` returns 8, so `card-img.js`'s padding normalisation would have cut reach ~99%; (2) `encodeURIComponent` leaves the apostrophe alone, so `Team Rocket's …` shipped 68 unverifiable links in the first pass — the `epn-params` gate failed the build, and `'` is now `%27`. AG variant (#33) checked before shipping, not after: Umbreon 161/131 = 329 plain / 145 under `LH_AV=1`, a real book, so the 17 $200+ rows carry it with a labelled row and one line of fine print naming the program as eBay's. `card-img.js` repoints a row to the cheapest verified listing it already fetched (43 of 49 on PRIS25 in the live check); AG rows are deliberately left on their filtered search. Gates all clean, `remark-indices --dry` clean on all six, levels unchanged. Kill: Oct 21 read, per-card IDs 0 actions on ≥30 clicks combined → drop the column, keep the encoding fix. |
| 37 | 2026-09-22 | **Basket floor for strip primaries on class landers** — a `buy-strip.json` rule (and a `buy-strip-health` check): a primary that is a broad singles search with a sub-$10 median is a config defect where the class has sealed product; the sealed box becomes the primary (`live:true`), the singles/hero link the secondary. First on `/nfl-rookie-cards-2026` — the best-converting lander of any size (54 sessions / 16.7% KE, 28d) selling a $1.44–$3.99 class (~$0.09 a converted click) — with the **2026 Bowman Football hobby box** (11/12 priced $379.99–$400, 2 autos/box, ships Sep 30; query needs `-chrome -jumbo` + `req`), `customid=nfl-rookie-cards-2026-box`, `-top`/`-hero` kept as the control (R14). Second pass after the read: `/wnba-cards-2026`, `/world-cup-2026-soccer-cards`. NOT the `best-*-under-50` pages (retail intent by construction) | $/click | M0 | **proposed** | `claude/ideas/2026-09-22.md`. Evidence: GA4 rates; EPN Sep 4–17 `bowman-football` 2 clicks → $14.75 → $0.45 (a football click at retail basket); the Sep 19 football shelf's Finding 1 (the $371 box converts at 1.1% on `/bowman-football`, Oct 3 re-read) is the counter-evidence, which is why the cheaper secondary stays. ESTIMATE +$3–$20/mo NFL, +$2–$10 WNBA/WC. Cost $0, Mo-time 0. Before Sep 30 if adopted. Kill: Oct 21 read, `-box` 0 actions on ≥20 clicks or the page's KE rate under 8% on ≥40 sessions → restore, and the rule is not rolled to the other two. **Flag 2 CLOSED 2026-09-22 (`9c3fa25`), and the browser check changed the conclusion: the page was printing `ask from $199.95`, and that listing was a genuine Flagship box — the desk's "would read $199.99 under a Flagship label" was not true. What WAS true: 15 of 40 passing listings were Heritage boxes at $199.99–$205 against a $199.95 Flagship low, so the figure sat one sold listing from being another product and the "39 live" depth was 38% Heritage. Fixed with `-heritage` + `req`/`deny` (39 live after, all Flagship, ask unchanged). Two smaller leaks closed the same pass (Update Series into Series 1, Finest into Topps Chrome Baseball). **The gate gap is the keeper:** `buy-strip-health` checked whether a shelf was EMPTY, never whether it held the RIGHT PRODUCT — it now reports WRONG-PRODUCT and UNGUARDED, self-tested both ways. Swept all 31 live shelves: 29 carry no guard, none is wrong today. See `claude/cos/buy-strip-product-guard-2026-09-22.md`. **Addendum same day (`568fc10`), on Mo's "name the product" call: choosing one for `pokemon-tcg-2026` exposed that `/api/comps` was being called with NO category, so every Pokémon shelf was searched in the sports-card default where Pokémon sealed is barely listed — Prismatic printed $197.99 on 2 listings against $135.00 on 43 under category 183456, and all five Pokémon pages were 13–47% high. Optional `cat` now flows config → `data-bs-cat` → client/builder/health; the 26 non-Pokémon shelves are untouched. The hub names the Phantasmal Flames ETB (9 live from $149, vs the 30th Celebration ETB's 2 from $200 mid-stagger). Health: thin 3 → 1, all other states 0. **Corrected within the hour (`3850e99`): Phantasmal Flames is a 2025 set (our own PF25 page says released Nov 14 2025; ticker PF25 like PRIS25/DR25), so the 2026 hub now sells the 30th Celebration ETB instead — 11 live from $180, 3.6× its $49.99 MSRP — and the 'missing Phantasmal row' content fix was NOT made, because the 2026 timeline is right to omit a 2025 set. Also shipped: the 30th page's 12 product links shared ONE custom ID across $14.99–$179.99 of products; each now has its own, and its live-ask tiles pass the sealed category. Method note: the house exclusion string in `_nkw` cut that shelf 17 raw → 4 — negative keywords belong in req/deny, not the query.** |
